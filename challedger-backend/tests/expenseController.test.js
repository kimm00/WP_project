const expenseController = require('../controllers/expenseController');

describe('expenseController', () => {
  test('expenseController object should be defined', () => {
    expect(expenseController).toBeDefined();
  });
});
